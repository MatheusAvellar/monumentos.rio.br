---
layout: default.ejs
title: Monumentos Rio
---
<%_ const monuments = collections.all.filter(file => file.filePathStem.startsWith("/id/"));
const monument_count = monuments.length;

const distinct_bairros = new Set();
monuments.forEach(m => { distinct_bairros.add(m.data.bairro); });
%>
<main>
<section class="maxwidth">
	<p>Bem vindo ao <strong><%= website_domain %></strong>!</p>
	<p>
		Este website tem como objetivo formar um inventário de todos os monumentos
		da cidade do Rio de Janeiro.
	</p>
	<p>
		Selecione a aba "Inventário" acima psara ver a lista completa de monumentos
		já catalogados. O projeto possui, no momento,
		<strong><%= monument_count %></strong> monumentos de
		<strong><%= distinct_bairros.size %></strong> bairros distintos.
	</p>
	<p>
		Alternativamente, escolha uma das categorias usadas para classificar cada
		monumento. Elas são as seguintes:
		<%- makeTag("obra") %> <%- makeTag("busto") %> <%- makeTag("chafariz") %>
		<%- makeTag("estátua") %> <%- makeTag("marco") %> <%- makeTag("escultura") %>
	</p>
	<br><hr><br>
	<p>Aqui estão alguns monumentos escolhidos aleatoriamente:</p>
</section>
<%_
// [Ref] https://bost.ocks.org/mike/shuffle/
function shuffle(array) {
	let m = array.length, t, i;
	while(m) {
		i = Math.floor(Math.random() * m--);
		t = array[m];
		array[m] = array[i];
		array[i] = t;
	}
	return array;
}
%>
<div class="card-list">
	<%_ for(let i = 0; i < 9; i++) { %>
		<%- makeCard(monuments[i]) %>
	<%_ } %>
</div>
</main>